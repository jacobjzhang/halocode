import { Factory } from 'ember-cli-mirage';

export default Factory.extend({
	text: 'The idea is to do a preorder traversal of the tree. In the preorder traversal, keep track of the value calculated till the current node, let this value be val. For every node, we update the val as val10 plus node’s data.*'	
});
